import Link from "next/link";
import { FiArrowLeft, FiAward } from "react-icons/fi";
import { getLeaderboard } from "@/src/lib/actions/mathquiz";

export default async function LeaderboardPage() {
  const rows = await getLeaderboard();
  return <main className="mx-auto max-w-3xl px-4 py-10 sm:px-6 lg:px-8"><Link href="/anggota/mathquiz" className="inline-flex items-center gap-2 text-sm font-semibold text-blue-700 hover:underline"><FiArrowLeft />Kembali ke MathQuiz</Link><div className="mt-6 rounded-3xl bg-gradient-to-br from-blue-950 to-cyan-700 p-8 text-white shadow-lg"><FiAward className="h-8 w-8 text-cyan-200" /><h1 className="mt-4 text-3xl font-bold">Leaderboard</h1><p className="mt-2 text-sm text-blue-100">Peringkat berdasarkan total poin terbaik dari setiap kuis.</p></div><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{rows.map((row, index) => <div key={row.user.fullName + index} className="flex items-center justify-between border-b border-slate-100 px-5 py-4 last:border-0"><div className="flex items-center gap-4"><span className={`flex h-9 w-9 items-center justify-center rounded-full text-sm font-bold ${index === 0 ? "bg-amber-100 text-amber-700" : index === 1 ? "bg-slate-200 text-slate-700" : index === 2 ? "bg-orange-100 text-orange-700" : "bg-blue-50 text-blue-700"}`}>{index + 1}</span><div><p className="font-semibold text-slate-900">{row.user.fullName}</p><p className="text-xs text-slate-500">{row.totalQuiz} kuis · {row.totalBenar} benar</p></div></div><p className="font-bold text-slate-900">{row.totalPoin} poin</p></div>)}{rows.length === 0 && <p className="p-8 text-center text-slate-600">Belum ada skor.</p>}</div></main>;
}